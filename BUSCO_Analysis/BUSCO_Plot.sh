#Program: BUSCO v3.0.2

#command:
#need to remove <> prior to running script
#I moved the summary files from BUSCO to new directories so only the summaries I wanted would be plotted
#I had to run the R script created from this script separately after changing a few parameters

python generate_plot.py -wd <working directory with summary files>

